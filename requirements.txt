numpy>=1.26,<2.0
pandas>=2.2,<3.0
prophet==1.1.5
# Exakt gepinnt (Georg 13.08.26: Charts nach dem Sprung 1.58 -> 1.61 geprueft).
# [auth] zieht Authlib + httpx fuer st.login mit: Streamlit laedt beides erst
# beim Klick nach, ein fehlendes Paket faellt sonst erst dort als 500 auf.
# Ausserdem: <1.36 rendert Charts mit Plotly 6 falsch.
streamlit[auth]==1.61.1
yfinance>=0.2.35
plotly>=5.18.0
